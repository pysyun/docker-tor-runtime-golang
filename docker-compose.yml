version: "3.8"

services:
  golang-tor-runtime:
    image: pysyun/golang-tor-runtime
    privileged: true
    deploy:
      mode: replicated
      replicas: 17
    environment:
      - INTERVAL=1
      - TARGET=https://httpbin.org/get
    command: ["/bin/sh", "-c", "curl $${TARGET}"]
